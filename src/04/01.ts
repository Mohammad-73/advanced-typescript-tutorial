// // type ThemeConfig = {
// //   darkMode: boolean;
// //   fontSize: number;
// //   primaryColor: string;
// // };

// // type PartialThemeConfig = {
// //   darkMode?: boolean;
// //   fontSize?: number;
// //   primaryColor?: string;
// // };

// const defaultThemeConfig = {
//   darkMode: false,
//   fontSize: 16,
//   primaryColor: "#333",
//   secondaryColor: "#666",
// };

// const applyTheme = (configs?: Partial<typeof defaultThemeConfig>) => {
//   const allConfigs = { ...defaultThemeConfig, ...configs };
//   console.log(allConfigs);
// };

// applyTheme({ darkMode: true });
