// Add quokka extension to vscode and run it with ctrl + shift + P.

// ----- Setting up the development environment -----
// const add = (a, b) => a + b;
// const add = (a: number, b: number) => a + b; // TS Friendly

// console.log(add(5, 2));

// ----- Introduce two important typescript configurations -----
// const multiply = (a:number, b:number) => a * b;

// const getLength = (str: string) => str.length;

// ----------

// const getLength = (str: string | undefined) => {
//   if (str === undefined) {
//     return 0;
//   }
//   return str.length;
// }; // TS Friendly

// console.log(getLength(undefined));

// ----------

// const enrolledCoursed = ["typescript", "websocket", "next.js"];

// console.log(enrolledCoursed[3].toLowerCase());
