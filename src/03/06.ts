// type ButtonType = "regular" | "icon" | "circle";

// declare function renderButton(type: ButtonType): void;

// renderButton("icon");

// // let iconButton = "icon";
// const iconButton = "icon"
// renderButton(iconButton);

// ------------

// declare function setColor(color: readonly [number, number, number]): void;

// setColor([255, 0, 0]);

// const redColor = [255, 0, 0] as const;
// setColor(redColor);
